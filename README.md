# Portafolio — Proyecto generado

Plantilla mínima para mostrar tus proyectos locales.

## Estructura
- `index.html` — página principal del portafolio
- `styles.css`, `main.js` — estilo e interacciones
- `assets/` — imágenes / capturas (SVGs de ejemplo)

## Cómo ver localmente
1. Abre `index.html` en tu navegador (doble clic), o
2. Sirve con un servidor estático desde la carpeta `portfolio`, por ejemplo:
   - `npx serve .` (requiere node) o
   - `python -m http.server 8000` (requiere Python)

## Links a tus proyectos
El portafolio enlaza a:
- `../gimnasio-landing/index.html`
- `../restaurant-mockup/index.html`

Si prefieres que los enlaces apunten a URLs públicas (GitHub Pages, Netlify, Vercel), dime las URLs y las actualizo.

## Despliegue a GitHub Pages
1. Crea un repo y sube el contenido de `portfolio/`.
2. Activa GitHub Pages desde la rama `main` o `gh-pages` (Settings → Pages).

---
Si quieres, puedo:
- Actualizar las descripciones y tomar capturas reales de cada proyecto
- Inicializar un repositorio git y hacer el primer commit
- Configurar deploy a GitHub Pages o Netlify

Dime qué prefieres y lo continuo 😊